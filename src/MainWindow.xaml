<Window x:Class="BlueBridge.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="BlueBridge" Width="590" Height="720" MinWidth="540" MinHeight="680"
        Foreground="#F7F7FC" Background="#0C0E17" WindowStartupLocation="CenterScreen"
        WindowStyle="None" ResizeMode="CanResizeWithGrip" Closing="Window_Closing"
        TextOptions.TextFormattingMode="Display" UseLayoutRounding="True">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="40" CornerRadius="14" GlassFrameThickness="0"
                            ResizeBorderThickness="6" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>
    <Window.Resources>
        <SolidColorBrush x:Key="Accent" Color="#7C5CFF"/>
        <SolidColorBrush x:Key="AccentHover" Color="#8F75FF"/>
        <SolidColorBrush x:Key="Surface" Color="#171925"/>
        <SolidColorBrush x:Key="SurfaceHover" Color="#222433"/>
        <SolidColorBrush x:Key="Stroke" Color="#2A2D3D"/>
        <SolidColorBrush x:Key="Muted" Color="#9EA3B7"/>

        <Style TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI"/>
        </Style>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Foreground" Value="#F8F8FF"/>
            <Setter Property="Background" Value="#202230"/>
            <Setter Property="BorderBrush" Value="#303344"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="16,11"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="ButtonBorder" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.90"/><Setter TargetName="ButtonBorder" Property="BorderBrush" Value="#555A70"/></Trigger>
                            <Trigger Property="IsPressed" Value="True"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.82"/></Trigger>
                            <Trigger Property="IsEnabled" Value="False"><Setter TargetName="ButtonBorder" Property="Opacity" Value="0.48"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AccentButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="{StaticResource Accent}"/>
            <Setter Property="BorderBrush" Value="#927BFF"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True"><Setter Property="Background" Value="{StaticResource AccentHover}"/></Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="MiniButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Padding" Value="12,6"/><Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="#1B1D2A"/>
        </Style>

        <Style x:Key="CaptionButton" TargetType="Button">
            <Setter Property="Width" Value="44"/><Setter Property="Height" Value="38"/>
            <Setter Property="Foreground" Value="#C9CDDC"/><Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/><Setter Property="FontFamily" Value="Segoe Fluent Icons"/>
            <Setter Property="FontSize" Value="10"/><Setter Property="Cursor" Value="Hand"/>
            <Setter Property="shell:WindowChrome.IsHitTestVisibleInChrome" Value="True"/>
            <Setter Property="Template"><Setter.Value><ControlTemplate TargetType="Button">
                <Border x:Name="CaptionSurface" Background="{TemplateBinding Background}" CornerRadius="7" Margin="2">
                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <ControlTemplate.Triggers>
                    <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="CaptionSurface" Property="Background" Value="#272A3A"/></Trigger>
                    <Trigger Property="IsPressed" Value="True"><Setter TargetName="CaptionSurface" Property="Opacity" Value="0.72"/></Trigger>
                </ControlTemplate.Triggers>
            </ControlTemplate></Setter.Value></Setter>
        </Style>

        <Style x:Key="ReadableCombo" TargetType="ComboBox">
            <Setter Property="Foreground" Value="#171822"/>
            <Setter Property="Background" Value="#F5F6FA"/>
            <Setter Property="BorderBrush" Value="#F5F6FA"/>
            <Setter Property="FontFamily" Value="Segoe UI Variable Text, Segoe UI"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Padding" Value="12,9"/>
            <Setter Property="MinHeight" Value="46"/>
        </Style>

        <Style x:Key="ToggleSwitch" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#F6F6FC"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid Background="Transparent">
                            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="48"/></Grid.ColumnDefinitions>
                            <ContentPresenter VerticalAlignment="Center"/>
                            <Grid Grid.Column="1" Width="44" Height="24" VerticalAlignment="Center">
                                <Border x:Name="Track" CornerRadius="12" Background="#353847" BorderBrush="#484C5D" BorderThickness="1"/>
                                <Ellipse x:Name="Thumb" Width="18" Height="18" Margin="3" HorizontalAlignment="Left" Fill="#D9DCE8"/>
                            </Grid>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="Track" Property="Background" Value="{StaticResource Accent}"/>
                                <Setter TargetName="Track" Property="BorderBrush" Value="#9A86FF"/>
                                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="Thumb" Property="Fill" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Track" Property="Opacity" Value="0.88"/></Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="#0C0E17" BorderBrush="#2D3041" BorderThickness="1" CornerRadius="14" ClipToBounds="True">
      <Grid>
        <Grid.RowDefinitions><RowDefinition Height="40"/><RowDefinition Height="*"/></Grid.RowDefinitions>
        <Grid Grid.Row="0" Background="#11131E" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
            <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal" Margin="14,0,0,0" VerticalAlignment="Center">
                <Border Width="20" Height="20" CornerRadius="6" Background="#7657F8">
                    <TextBlock Text="&#xE8D4;" FontFamily="Segoe Fluent Icons" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="BlueBridge" FontSize="12" FontWeight="SemiBold" Margin="9,0,0,0" VerticalAlignment="Center"/>
            </StackPanel>
            <Button Grid.Column="1" Style="{StaticResource CaptionButton}" Content="&#xE921;" Click="Minimize_Click"/>
            <Button Grid.Column="2" Style="{StaticResource CaptionButton}" Content="&#xE8BB;" Click="Close_Click"/>
        </Grid>
        <Grid Grid.Row="1" Margin="30,20,30,18">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions><ColumnDefinition Width="Auto"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Border Width="48" Height="48" CornerRadius="14">
                    <Border.Background><LinearGradientBrush StartPoint="0,0" EndPoint="1,1"><GradientStop Color="#947FFF"/><GradientStop Color="#6040EA" Offset="1"/></LinearGradientBrush></Border.Background>
                    <TextBlock Text="&#xE8D4;" FontFamily="Segoe Fluent Icons" FontSize="22" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <StackPanel Grid.Column="1" Margin="14,1,0,0">
                    <TextBlock Text="BlueBridge" FontFamily="Segoe UI Variable Display, Segoe UI" FontSize="27" FontWeight="SemiBold"/>
                    <TextBlock Text="Phone audio, routed your way" Foreground="{StaticResource Muted}" FontSize="12" Margin="1,2,0,0"/>
                </StackPanel>
            </Grid>

            <Border Grid.Row="1" Background="{StaticResource Surface}" BorderBrush="{StaticResource Stroke}" BorderThickness="1" CornerRadius="18" Padding="20" Margin="0,0,0,16">
                <Grid>
                    <Grid.ColumnDefinitions><ColumnDefinition Width="64"/><ColumnDefinition Width="*"/><ColumnDefinition Width="Auto"/></Grid.ColumnDefinitions>
                    <Border Width="56" Height="72" CornerRadius="15" Background="#242638" BorderBrush="#303348" BorderThickness="1">
                        <TextBlock Text="&#xE8D6;" FontFamily="Segoe Fluent Icons" FontSize="25" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#957FFF"/>
                    </Border>
                    <StackPanel Grid.Column="1" Margin="16,5,10,3">
                        <TextBlock x:Name="StatusTitle" Text="Ready" FontSize="18" FontWeight="SemiBold"/>
                        <TextBlock x:Name="StatusDevice" Text="Choose a Bluetooth audio device" FontSize="13" Margin="0,5,0,0"/>
                        <TextBlock x:Name="StatusMessage" Text="Ready when you are" Foreground="{StaticResource Muted}" FontSize="11" Margin="0,5,0,0"/>
                    </StackPanel>
                    <Border Grid.Column="2" Background="#242635" CornerRadius="12" Padding="10,6" VerticalAlignment="Top">
                        <TextBlock x:Name="LiveText" Text="IDLE" FontWeight="Bold" FontSize="10" Foreground="#AEB2C3"/>
                    </Border>
                </Grid>
            </Border>

            <StackPanel Grid.Row="2">
                <Grid Margin="1,0,0,8">
                    <TextBlock Text="BLUETOOTH AUDIO DEVICE" Foreground="#A796FF" FontSize="10" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Style="{StaticResource MiniButton}" HorizontalAlignment="Right" Click="RefreshPhones_Click"><StackPanel Orientation="Horizontal"><TextBlock Text="&#xE72C;" FontFamily="Segoe Fluent Icons" Margin="0,0,7,0"/><TextBlock Text="Refresh"/></StackPanel></Button>
                </Grid>
                <Border CornerRadius="11" Background="#F5F6FA" BorderBrush="#D9DCE7" BorderThickness="1" ClipToBounds="True" Margin="0,0,0,13">
                    <ComboBox x:Name="PhoneCombo" Style="{StaticResource ReadableCombo}" BorderThickness="0" DisplayMemberPath="DisplayName" SelectionChanged="PhoneCombo_SelectionChanged"/>
                </Border>
                <Grid Margin="1,0,0,8">
                    <TextBlock Text="PLAY SOUND THROUGH" Foreground="#A796FF" FontSize="10" FontWeight="Bold" VerticalAlignment="Center"/>
                    <Button Style="{StaticResource MiniButton}" HorizontalAlignment="Right" Click="RefreshOutputs_Click"><StackPanel Orientation="Horizontal"><TextBlock Text="&#xE72C;" FontFamily="Segoe Fluent Icons" Margin="0,0,7,0"/><TextBlock Text="Refresh"/></StackPanel></Button>
                </Grid>
                <Border CornerRadius="11" Background="#F5F6FA" BorderBrush="#D9DCE7" BorderThickness="1" ClipToBounds="True" Margin="0,0,0,14">
                    <ComboBox x:Name="OutputCombo" Style="{StaticResource ReadableCombo}" BorderThickness="0" DisplayMemberPath="DisplayName" SelectionChanged="OutputCombo_SelectionChanged"/>
                </Border>
            </StackPanel>

            <Grid Grid.Row="3" Margin="0,0,0,16">
                <Grid.ColumnDefinitions><ColumnDefinition Width="1.75*"/><ColumnDefinition Width="12"/><ColumnDefinition Width="*"/></Grid.ColumnDefinitions>
                <Button x:Name="ConnectButton" Style="{StaticResource AccentButton}" Click="Connect_Click">
                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE768;" FontFamily="Segoe Fluent Icons" FontSize="14" Margin="0,0,8,0"/><TextBlock x:Name="ConnectButtonText" Text="Connect device audio"/></StackPanel>
                </Button>
                <Button Grid.Column="2" Style="{StaticResource ModernButton}" Click="Repair_Click" ToolTip="Closes and opens the audio stream exactly once">
                    <StackPanel Orientation="Horizontal"><TextBlock Text="&#xE777;" FontFamily="Segoe Fluent Icons" FontSize="14" Margin="0,0,8,0"/><TextBlock Text="Repair audio"/></StackPanel>
                </Button>
            </Grid>

            <Border Grid.Row="4" Background="{StaticResource Surface}" BorderBrush="{StaticResource Stroke}" BorderThickness="1" CornerRadius="18" Padding="18,16">
                <StackPanel>
                    <CheckBox x:Name="AutoConnectCheck" Style="{StaticResource ToggleSwitch}" Checked="SettingChanged" Unchecked="SettingChanged">
                        <StackPanel><TextBlock Text="Connect when BlueBridge starts" FontWeight="SemiBold" FontSize="13"/><TextBlock Text="Uses your last device and output once at startup" Foreground="{StaticResource Muted}" FontSize="11" Margin="0,3,0,0"/></StackPanel>
                    </CheckBox>
                    <Separator Background="#2A2D3D" Margin="0,13"/>
                    <CheckBox x:Name="StartupCheck" Style="{StaticResource ToggleSwitch}" Checked="StartupChanged" Unchecked="StartupChanged">
                        <StackPanel><TextBlock Text="Start with Windows" FontWeight="SemiBold" FontSize="13"/><TextBlock Text="Starts quietly in the notification area" Foreground="{StaticResource Muted}" FontSize="11" Margin="0,3,0,0"/></StackPanel>
                    </CheckBox>
                    <Separator Background="#2A2D3D" Margin="0,13"/>
                    <CheckBox x:Name="TrayCheck" Style="{StaticResource ToggleSwitch}" Checked="SettingChanged" Unchecked="SettingChanged">
                        <StackPanel><TextBlock Text="Close to tray" FontWeight="SemiBold" FontSize="13"/><TextBlock Text="Keeps audio running without a taskbar window" Foreground="{StaticResource Muted}" FontSize="11" Margin="0,3,0,0"/></StackPanel>
                    </CheckBox>
                </StackPanel>
            </Border>

            <TextBlock Grid.Row="5" Text="Your device. Your sound. Your route." Foreground="#777C92" HorizontalAlignment="Center" FontSize="10" Margin="0,12,0,0"/>
        </Grid>
      </Grid>
    </Border>
</Window>
